# 细碎优化计划 — 冷缓存与热缓存

## 基准

- **冷缓存 nrx=1**: 7.5s（首次 jit 编译占 ~66%）
- **热缓存 nrx=1**: 2.79s（jit 已编译）
- **冷缓存 nrx=2**: 4.2s
- **热缓存 nrx=2**: —（待测）

---

## 冷缓存优化

冷缓存总时间 10.6s，其中 numba jit 编译占 7.0s（66%）。

| # | 方案 | 预估收益 | 风险 |
|:--:|------|:--:|:--:|
| 1 | 给所有 `@numba.jit` 加 `cache=True`，首次编译后磁盘缓存 | ~7.0s → 0s（后续启动） | 低 |
| 2 | hotpants.py 的 `fill_stamp_numba_kernel_local` 与 alard.py 的 `fill_stamp_numba_kernel` 功能重复，合并为一个 | ~0.5s | 中 |
| 3 | 减少 jit 函数总数（合并 build_matrix0_jit + build_matrix_jit 等） | ~0.3s | 中 |

---

## 热缓存优化

热缓存总时间 2.79s。按收益排序：

| # | 热点 | 当前耗时 | 优化方案 | 预估收益 |
|:--:|------|:---:|------|:--:|
| 1 | `numpy.asarray` 691次 | 0.28s | fill_stamp_numba 内 img_flat/imRef_flat 每次 ravel，改为外部预计算 | ~0.15s |
| 2 | `numpy.copy` 1365次 | 0.25s | buildStampsNumba 的 lmRData.copy() 用视图代替 | ~0.10s |
| 3 | `sigma_clip_numpy` 122次 | 0.17s | 加 @numba.jit | ~0.10s |
| 4 | `background_loop_jit` | 已 jit | 加 prange（每像素独立无竞态） | ~0.01s |
| 5 | fill_stamp_numba 内 `renFlags` 构建 | — | list → numpy array 一次性转换 | ~0.01s |
| 6 | `build_matrix_numpy` 17次 | 0.65s | 累加竞争无法 prange | — |
| 7 | `bin_quartile_numpy` 312次 | ~0.002s | 加 @numba.jit | ~0.001s |

热缓存总计预估收益：**~0.38s**，可从 2.79s 降到 **~2.4s**。

---

## 冷+热组合预估

| 场景 | 当前 | 优化后 |
|------|:---:|:---:|
| nrx=1 首次运行 | 7.5s | ~3.0s |
| nrx=1 二次运行 | 2.79s | ~2.4s |
| nrx=2 首次运行 | 4.2s | ~1.8s |
| nrx=2 二次运行 | — | ~1.5s |

---

## 执行顺序

1. **冷 #1**: 全部 jit 加 `cache=True`（最大收益，最低风险）
2. **热 #1**: fill_stamp_numba 去 ravel 预计算
3. **热 #2**: lmRData 视图
4. **热 #3**: sigma_clip jit
5. 其余小项

---

## 基准时间 (2026-06-22 23:15, commit 96e333a)

| 配置 | 冷态 | 热态 |
|------|:---:|:---:|
| nrx=1 nry=1 | 8.79s | 2.85s |
| nrx=2 nry=2 | 10.56s | 4.39s |
