# C 风格原地修改模式分析报告

> 分析范围: functions.py (1251L), alard.py (2364L), hotpants.py (~5200L)
> 排除: @numba.jit 函数体内的赋值, 注释代码, deprecated/ 目录

---

## 统计

| 文件 | ndarray原地修改 | out参数模式 | dict/list原地修改 | 闭包修改 | 合计 |
|------|:---:|:---:|:---:|:---:|:---:|
| functions.py | 8 | 1 | 0 | 0 | 9 |
| alard.py | 10 | 1 | 0 | 1 | 12 |
| hotpants.py | 11 | 1 | 0 | 1 | 13 |
| **总计** | **29** | **3** | **0** | **2** | **34** |

注: hotpants.py 与 functions.py/alard.py 有大量函数同名重实现（不同参数解构方式），各文件独立计数。

---

## 🔴 高风险 — 多 region 扁平数组被大面积切片写回

### 文件: alard.py

#### 函数: fill_stamp_numba (line 880)
- **模式**: ndarray原地修改 + out参数模式
- **修改内容**:
  ```python
  saVectors[si, :nvec + nbg, :fwSqStamp] = out_vectors
  saKrefArea[si, :] = out_krefArea
  saMat[si, :nC, :nC] = out_mat
  saScprod[si, :nC] = out_scprod
  saSumVal[si] = out_sum_val[0]
  ```
- **风险**: 函数内部通过 jit kernel 计算出 out_vectors/out_mat/out_scprod/out_krefArea，然后将结果整体切片写回传入的 5 个扁平 ndarray 参数。调用方传入的这些数组的 `[si, ...]` 切片被完全覆盖。这是整个系统中最核心的"out参数批量写回"模式。

#### 函数: fill_stamp_numpy (line 765)
- **模式**: ndarray原地修改（委托）
- **修改内容**: 直接 `return fill_stamp_numba(...)`，风险同上
- **风险**: 调用方需预分配 saVectors/saMat/saScprod/saKrefArea/saSumVal 全部内存

#### 函数: check_again_numpy (line 1971)
- **模式**: ndarray原地修改（状态机推进）
- **修改内容**:
  ```python
  saSscnt[istamp] += 1          # line 2101, 2134
  saChi2[istamp] = sig          # line 2116
  ```
- **风险**: 接收扁平数组 saSscnt/saChi2 作为参数，在循环中直接修改调用方的 stamp 计数器状态和 chi2 值。这是典型的 C 风格"state machine in-place advancement"模式。

#### 函数: build_matrix_numpy (line 480)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  wxy.fill(0.0)                 # line 533
  wxy[i, j] = 0.0               # line 505 (n_valid==0 分支)
  ```
- **风险**: 接收调用方分配的 wxy 数组，体内 fill(0.0) 和直接下标赋值污染原数组。

### 文件: hotpants.py

#### 函数: fill_stamp_numba (line 3306)
- **模式**: ndarray原地修改 + out参数模式
- **修改内容**: 同 alard.py 中的 fill_stamp_numba（line 3360-3368），完全一致的 5 个数组切片写回
- **风险**: 同 alard.py 版本

#### 函数: check_again_numpy (line 4619)
- **模式**: ndarray原地修改（状态机推进）
- **修改内容**:
  ```python
  saSscnt[istamp] += 1          # line 4749, 4782
  saChi2[istamp] = sig          # line 4764
  ```
- **风险**: 同 alard.py 版本，状态机 in-place

#### 函数: build_matrix_numpy (line 2673)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 2727, 2699）
- **风险**: wxy 参数污染

---

## 🟡 中风险 — 单 region / 单个数组被直接下标修改

### 文件: functions.py

#### 函数: insert_subregion_flt_numpy (line 209)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  full_2d[fpixelY-1:fpixelY-1+pixY, fpixelX-1:fpixelX-1+pixX] = sub_2d[yBufLo:yBufLo+pixY, xBufLo:xBufLo+pixX]
  ```
- **风险**: 直接修改传入的 full_2d 参数内容，调用方必须确保 full_2d 是可写的

#### 函数: insert_subregion_int_numpy (line 217)
- **模式**: ndarray原地修改
- **修改内容**: 同 insert_subregion_flt_numpy（line 220-222），操作 int 类型 full_2d
- **风险**: 同 flt 版本

#### 函数: cut_sstamp_numpy (line 460)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  saKrefArea[si, :] = fillVal                              # line 461
  saKrefArea[si, x + y * fwKSStamp] = dpt                  # line 479
  saSumVal[si] = sumVal                                     # line 483
  ```
- **风险**: 修改传入的 saKrefArea[si] 和 saSumVal[si]

#### 函数: check_psf_center_numpy (line 521)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  mRData[nr2] = int(mRData[nr2]) | bbit1                   # line 552
  ```
- **风险**: 标记 hot pixel 时直接或写入 mRData，调用方 mask 数据被污染

#### 函数: get_psf_centers_numpy (line 599)
- **模式**: ndarray原地修改 (通过对象属性)
- **修改内容**:
  ```python
  mRData[nr] = int(mRData[nr]) | bbit1                     # line 646
  mRData[nr2] = int(mRData[nr2]) | bbit1                   # line 676
  mRData[nr2] = int(mRData[nr2]) | bbit2                   # line 709
  sa.xss[si, idx] = xloc[...] + sx0                        # line 731
  sa.yss[si, idx] = yloc[...] + sy0                        # line 732
  sa.nss[si] += 1                                           # line 733
  ```
- **风险**: 同时修改 mRData(flag mask)、sa.xss、sa.yss、sa.nss 共 4 个不同数组/属性

#### 函数: get_stamp_stats3_fast_numpy (line 290)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  mRData_2d[nan_y + y0Reg, nan_x + x0Reg] |= (FLAG_INPUT_ISBAD | FLAG_ISNAN)  # line 348
  ```
- **风险**: 发现 NaN 时直接修改调用方的 mRData_2d mask

#### 函数: ludcmp_numpy (line 1126)
- **模式**: ndarray原地修改 (经典数值算法)
- **修改内容**:
  ```python
  a[i, j] = sum_val                                         # line 1147, 1154
  a[imax, k] = a[j, k]; a[j, k] = dum                      # line 1163-1164
  indx[j] = imax                                            # line 1168
  a[j, j] = TINY                                            # line 1170
  a[i, j] *= dum                                            # line 1174
  ```
- **风险**: 原地 LU 分解，输入矩阵 a 被覆盖为 L+U 复合形式，indx 记录行交换

#### 函数: lubksb_numpy (line 1107)
- **模式**: ndarray原地修改 (经典数值算法)
- **修改内容**:
  ```python
  b[ip] = b[i]; b[i] = sum_val                             # line 1112, 1118
  b[i] = sum_val / a[i, i]                                  # line 1123
  ```
- **风险**: 原地回代，输入 b 被覆盖为解向量

### 文件: alard.py

#### 函数: make_kernel_numpy (line 87)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  kernel_coeffs[i1] = coeff                                 # line 102
  kernel[i] = 0.0                                           # line 106
  kernel[i] = val                                           # line 112
  ```
- **风险**: 接收 kernel 和 kernel_coeffs 作为输出缓冲区，完全覆盖内容

#### 函数: kernel_vector_numpy (line 129)
- **模式**: ndarray原地修改 + list原地修改
- **修改内容**:
  ```python
  filter_x[k] = qe * math.pow(x, deg_x)                    # line 146
  filter_y[k] = qe * math.pow(x, deg_y)                    # line 147
  filter_x[ix + n * fwKernel] *= sum_x                      # line 175
  filter_y[ix + n * fwKernel] *= sum_y                      # line 176
  ```
- **风险**: 全局共享的 filter_x/filter_y 数组被直接写入

#### 函数: kernel_vector_pca_numpy (line 116)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  vector[i] -= kernel0[i]                                   # line 125
  ```
- **风险**: 局部创建的 vector 后被 append 到 kernel_vec list，向量被原地减法修改

#### 函数: xy_conv_stamp_fast_numba (line 261)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  saVectors[si, n, :fwKSStamp * fwKSStamp] = out_vec[n]    # line 284
  ```
- **风险**: 通过 jit kernel 计算后，整行写回 saVectors[si, n, :] 切片

#### 函数: xy_conv_stamp_pca_numpy (line 286)
- **模式**: ndarray原地修改 (通过对象属性)
- **修改内容**:
  ```python
  sa.vectors[si, n, xij] = 0.0                              # line 299
  sa.vectors[si, n, xij] += float(val_img * val_pca)       # line 305
  sa.vectors[si, n, i] -= sa.vectors[si, 0, i]             # line 310
  ```
- **风险**: 原地累加构造卷积结果，可能被多次调用覆盖之前值

#### 函数: spatial_convolve_numpy_fast (line 1202)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  cRdata2d[sy, sx] = output[sy, sx]                        # line 1272
  mRDataView[sy, sx] |= innerCMask                          # line 1307
  mRDataView[sy, sx] |= (FLAG_OUTPUT_ISBAD * badSelf.astype(np.int32))  # line 1309
  mRData[ni] = int(mRData[ni]) | ...                        # lines 1344,1346
  ```
- **风险**: cRdata 和 mRData 作为 in/out 参数，输出结果和 mask flag 直接写入

#### 函数: spatial_convolve_numpy (line 1356)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  cRdata[ni] = q                                            # line 1414
  vData[ni] = qv                                            # line 1416
  mRData[ni] = int(mRData[ni]) | ...                        # lines 1418-1425
  ```
- **风险**: 三重输出 (卷积结果、方差、mask) 全部原地写入外部数组

#### 函数: check_stamps_numpy (line 1760)
- **模式**: ndarray原地修改
- **修改内容**:
  ```python
  saNorm[i] = sum_val                                       # line 1802
  saDiff[i] = abs((saNorm[i] - kmean) / kstdev)            # line 1810
  ```
- **风险**: 直接写入调用方的 saNorm/saDiff 数组

### 文件: hotpants.py

#### 函数: cut_sstamp_numpy (line 948)
- **模式**: ndarray原地修改
- **修改内容**: 同 functions.py 版本（line 950, 968, 972）
- **风险**: 同 functions.py 版本

#### 函数: check_psf_center_numpy (line 1010)
- **模式**: ndarray原地修改
- **修改内容**: 同 functions.py 版本（line 1041）
- **风险**: 同 functions.py 版本

#### 函数: get_psf_centers_numpy (line 1088)
- **模式**: ndarray原地修改 (通过对象属性)
- **修改内容**: 同 functions.py 版本（line 1147, 1177, 1210, 1235-1239）
- **风险**: 同 functions.py 版本

#### 函数: get_stamp_stats3_fast_numpy (line 768)
- **模式**: ndarray原地修改
- **修改内容**: 同 functions.py 版本（line 830）
- **风险**: 同 functions.py 版本

#### 函数: spatial_convolve_numpy_fast (line 3684)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 3756, 3791-3793, 3828-3830）
- **风险**: 同 alard.py 版本

#### 函数: spatial_convolve_numpy (line 3841)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 3899-3908）
- **风险**: 同 alard.py 版本

#### 函数: check_stamps_numpy (line 4401)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 4443-4451）
- **风险**: 同 alard.py 版本

#### 函数: make_kernel_numpy (line 2082)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 2097, 2101-2107）
- **风险**: 同 alard.py 版本

#### 函数: kernel_vector_numpy (line 2145)
- **模式**: ndarray原地修改
- **修改内容**: 同 alard.py 版本（line 2162-2177）
- **风险**: 同 alard.py 版本

#### 函数: lubksb_numpy (line 2112) / ludcmp_numpy (line 2210)
- **模式**: ndarray原地修改
- **修改内容**: 同 functions.py 版本
- **风险**: 同 functions.py 版本

---

## 🟢 低风险 — 局部作用域变体的原地修改（风险可控但仍是 C 风格）

### 文件: functions.py

#### 函数: buildStampsNumba (line 913)
- **模式**: ndarray原地修改（大量扁平 1d 参数）
- **修改内容**: ctX0[ntS] = x0, ctY0[ntS] = y0, ctX[ntS] = cx, ctY[ntS] = cy, ctSumVal[ntS], ctMeanVal[ntS], ctMedian[ntS], ctMode[ntS], ctSd[ntS], ctFwhm[ntS], ctLfwhm[ntS], ctXss[ntS, idx], ctYss[ntS, idx], ctNss[ntS], ciX0[niS], ciY0[niS], ciX[niS], ciY[niS], ciSumVal[niS], ciMeanVal[niS], ciMedian[niS], ciMode[niS], ciSd[niS], ciFwhm[niS], ciLfwhm[niS], ciXss[niS, idx], ciYss[niS, idx], ciNss[niS], mRData1d[...]
- **风险**: 函数签名接收 40+ 个扁平 ndarray 参数，全部在函数体内被直接下标赋值。这是整个项目中参数数量最多、原地修改最密集的函数，但风险可控因为调用方就是为此目的分配的这些数组。

### 文件: hotpants.py

#### 函数: buildStampsNumba (line 1745)
- **模式**: ndarray原地修改（大量扁平 1d 参数）
- **修改内容**: 同 functions.py 版本，完全一致的模式
- **风险**: 同 functions.py 版本

---

## 🔵 out 参数模式（显式输出缓冲区）

此类函数签名明确接收 out 参数作为输出容器，属于设计选择而非意外污染。

### 文件: functions.py

#### 函数: psfCentersVectorized (line 739)
- **模式**: out参数 (xloc_out, yloc_out, peaks_out) + ndarray原地修改 (mRData1d_2d)
- **修改内容**:
  ```python
  xloc_out[pcnt] = ip; yloc_out[pcnt] = jp; peaks_out[pcnt] = dmax_val  # 812-814
  mRData2d[jlo:jhi, ilo:ihi] = mRData1d_2d | bbit2                      # 820
  ```
- **风险**: 显式 out 参数设计，但同时也修改了 mRData2d (经由 base_valid 间接)

### 文件: alard.py + hotpants.py

#### 函数: fill_stamp_numba (alard.py:880, hotpants.py:3306)
- **模式**: out参数（内部局部 out_* 数组写入入参）
- 已在 🔴 高风险区域分析

---

## 🟣 闭包内修改非局部变量

### 文件: alard.py

#### 函数: fit_kernel_numpy (line 2157)
- **模式**: 闭包修改
- **修改内容**:
  ```python
  def do_fill(indices):
      for idx in indices:
          fill_stamp_numpy(saVectors, saMat, saScprod, ..., idx, ...)   # line 2211-2216
  ```
- **风险**: 内层函数 do_fill 读写外层 fit_kernel_numpy 定义的 saVectors/saMat/saScprod/saKrefArea/saSumVal/saSscnt/saNss/saXss/saYss 等 9 个扁平 ndarray。这些 via fill_stamp_numpy 最终被 fill_stamp_numba 原地覆盖。闭包捕捉了上一 frame 的引用，形成间接的原地修改链。

### 文件: hotpants.py

#### 函数: fit_kernel_numpy (line 4810)
- **模式**: 闭包修改
- **修改内容**: 同 alard.py 版本（line 4842-4847），从 dict sa 中提取的扁平数组被闭包 do_fill 捕获
- **风险**: 同 alard.py 版本

---

## 📊 汇总

### 按风险等级统计

| 风险等级 | 数量 | 代表模式 |
|----------|:----:|----------|
| 🔴 高 | 8 | fill_stamp_numba 五数组批量写回, check_again 状态机, build_matrix wxy 污染 |
| 🟡 中 | 18 | 单数组下标修改 (insert_subregion, spatial_convolve, ludcmp/lubksb 等) |
| 🟢 低 | 2 | buildStampsNumba 预分配数组填充 |
| 🔵 out | 3 | psfCentersVectorized, fill_stamp_numba |
| 🟣 闭包 | 2 | fit_kernel_numpy.do_fill |

### 核心问题总结

1. **多 region 污染链**: `fill_stamp_numba` → `fill_stamp_numpy` → `fit_kernel_numpy.do_fill` 形成三层嵌套的原地修改链，涉及 saVectors/saMat/saScprod/saKrefArea/saSumVal 共 5 个数组的批量切片覆盖。这是整个系统最核心的 C 风格残留。

2. **状态机 in-place**: `check_again_numpy` 直接将 `saSscnt[istamp] += 1` 作为状态推进手段，完全依赖调用方在内存中维护正确的计数器。

3. **mask 数组污染**: `check_psf_center_numpy`、`get_psf_centers_numpy`、`get_stamp_stats3_fast_numpy` 等多个统计/检测函数发现异常时直接或写调用方的 mRData mask 数组。

4. **三重输出缓冲区**: `spatial_convolve_numpy` 和 `spatial_convolve_numpy_fast` 同时对 cRdata(卷积结果)、vData(方差)、mRData(mask) 三个外部数组进行原地写入。

5. **LU 分解经典模式**: `ludcmp_numpy` + `lubksb_numpy` 遵循 Fortran/C 传统，原地覆盖输入矩阵 a 和右端向量 b。
